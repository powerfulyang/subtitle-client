# Project Memory

- UI implementation preference: when a suitable Ant Design component exists, prefer using `antd` instead of custom-built controls. Custom UI is still acceptable when `antd` does not fit the interaction or visual requirement cleanly.
